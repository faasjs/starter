defaults:
  plugins:
    http:
      config:
        cookie:
          secure: false
          session:
            secret: secret
    knex:
      config:
        client: pg
        pool:
          min: 0
          max: 10
development:
testing:
production:
